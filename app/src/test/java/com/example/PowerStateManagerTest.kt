package com.example

import org.junit.Assert.assertEquals
import org.junit.Test

class PowerStateManagerTest {

    @Test
    fun `deep idle state turns on device power save modes`() {
        assertEquals(true, true)
    }
}
